export const metadata = {
  title: 'Puru Eay - Next.js AI',
  description: 'AI Interface powered by PuruBoy API',
}

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body>{children}</body>
    </html>
  )
}